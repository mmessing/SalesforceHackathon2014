<!DOCTYPE html>

<html>
  <head>
    <title>Up Dog</title>
    <meta name="viewport" content="initial-scale = 1.0,maximum-scale = 1.0" />
    <link rel="stylesheet" href="/assets/application.css" />
  </head>
  <body>
    <nav>
      <%
        login_out = @user == nil ? 'login' : 'logout'

        {
          'home' => nil,
          'search' => 'listings',
          'host' => 'listings/new',
          'profile' => 'profile',
          'friends' => 'friends',
          'settings' => 'settings',
          login_out => login_out
        }.each do |name, link|
      %>
        <a
          href="<%= "/#{link}" %>"
          class="<%= 'selected' if request.path == "/#{link}" %>">
          <%= "#{name}" %>
        </a>
      <% end %>
      <% if @user %>
        <div id="users-name">Hello <%= @user['name'] %></div>
      <% end %>
    </nav>

    <header>
      <h1><a id='home' href="../">
        Up Dog<em>e</em></a>
        <button id='nav-toggle' title='wow'>&equiv;</button>
      </h1>
    </header>
    <%= yield %>
  </body>
</html>

<script src="/assets/application.js"></script>
<script src="https://maps.googleapis.com/maps/api/js"></script>
